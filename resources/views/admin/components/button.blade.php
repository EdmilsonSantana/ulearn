<button type="{{ $type }}" id="{{ isset($id) ? $id : '' }}" class="btn {{isset($large) && $large ? 'btn-lg btn-block' : ''}} btn-learna {{ $primary ? 'btn-learna-primary' : ''}}">{{ $text }}</button>
